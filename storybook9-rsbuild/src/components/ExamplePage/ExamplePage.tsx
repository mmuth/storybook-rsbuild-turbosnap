import { Grid2 } from '@mui/material';
import React from 'react';

export const ExamplePage: React.FunctionComponent = ({}) => {
  return (
    <Grid2>
      <p>This is a very simple example of Storybook using rsbuild</p>
    </Grid2>
  );
};
